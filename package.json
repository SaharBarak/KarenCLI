{
  "name": "karen-cli-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Karen CLI - CSS Layout Auditing Tool with Managed Service",
  "author": "Sahar Barak",
  "license": "MIT",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rm -rf node_modules",
    "cli": "pnpm --filter karen-cli",
    "backend": "pnpm --filter karen-backend",
    "web": "pnpm --filter karen-web"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.2",
    "@types/node": "^22.10.2",
    "prettier": "^3.4.2",
    "eslint": "^9.17.0"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
